<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Characters</title>
    <%- include("cdn.ejs") %>
    <!-- Font Awesome CDN -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      integrity="sha512-papTfNKe9tLz1Z6NKDQ9+5FEklAKq0ZQt6FtElMZT8IYi+Tb5QgXL8rUu4k7tZzPPYxwMePeB3Xvu98s+Z7GVA=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
    <style>
      .card-container {
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
        justify-content: center;
        padding: 20px 0;
      }
      .card {
        flex: 0 0 auto;
        width: 350px;
        height: 100%;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.15);
      }
      .card-img-top {
        height: 250px;
        object-fit: cover;
        border-top-left-radius: 10px;
        border-top-right-radius: 10px;
      }
      .action-btns {
        display: flex;
        justify-content: space-between;
        margin-top: 10px;
      }
    </style>
  </head>
  <body>
    <div class="container mt-4">
      <h1 class="text-center mb-4">One Piece Characters</h1>
      <hr />

      <% if (!characters || characters.length === 0) { %>
      <div class="alert alert-info text-center" role="alert">
        No characters found.
      </div>
      <% } else { %>
      <div class="card-container">
        <% characters.forEach((ch) => { %>
        <div class="card">
          <img
            src="/uploads/<%= ch.profile_pic %>"
            class="card-img-top"
            alt="Profile Picture"
          />
          <div class="card-body">
            <h5 class="card-title"><%= ch.name %></h5>
            <p class="card-text">
              <strong>Email:</strong> <%= ch.email %><br />
              <strong>Gender:</strong> <%= ch.gender %><br />
              <strong>Role:</strong> <%= ch.role %>
            </p>
            <div class="action-btns">
              <a href="/edit/<%= ch.id %>" class="btn btn-sm btn-warning">
                <i class="fas fa-edit"></i> Edit
              </a>
              <a
                href="/delete/<%= ch.id %>"
                class="btn btn-sm btn-danger"
                onclick="return confirm('Are you sure you want to delete this character?')"
              >
                <i class="fas fa-trash-alt"></i> Delete
              </a>
            </div>
          </div>
        </div>
        <% }) %>
      </div>
      <% } %>
    </div>
  </body>
</html>
