<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update Product</title>

    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container mt-5">
      <h2 class="text-center mb-4">Update Product</h2>
      <hr />

      <form action="/updateProduct" method="POST" class="border p-4 rounded shadow-sm">
        <input type="hidden" name="pid" value="<%- data.pid %>" />

        <div class="mb-3">
          <label for="pname" class="form-label">Product Name</label>
          <input
            type="text"
            class="form-control"
            name="pname"
            id="pname"
            value="<%- data.pname %>"
            required
          />
        </div>

        <div class="mb-3">
          <label for="pcategory" class="form-label">Product Category</label>
          <select class="form-select" name="pcategory" id="pcategory" required>
            <option value="None">Select</option>
            <option value="Electronics" <%- data.pcategory === 'Electronics' ? 'selected' : '' %>>Electronics</option>
            <option value="Groceerry" <%- data.pcategory === 'Groceerry' ? 'selected' : '' %>>Groceerry</option>
            <option value="Clothing" <%- data.pcategory === 'Clothing' ? 'selected' : '' %>>Clothing</option>
            <option value="Mens" <%- data.pcategory === 'Mens' ? 'selected' : '' %>>Mens</option>
            <option value="Kids" <%- data.pcategory === 'Kids' ? 'selected' : '' %>>Kids</option>
            <option value="Other" <%- data.pcategory === 'Other' ? 'selected' : '' %>>Other</option>
          </select>
        </div>

        <div class="mb-3">
          <label for="pprice" class="form-label">Product Price</label>
          <input
            type="text"
            class="form-control"
            name="pprice"
            id="pprice"
            value="<%- data.pprice %>"
            required
          />
        </div>

        <div class="mb-3">
          <label for="pquantity" class="form-label">Product Quantity</label>
          <input
            type="number"
            class="form-control"
            name="pquantity"
            id="pquantity"
            value="<%- data.pquantity %>"
            required
          />
        </div>

        <div class="mb-3">
          <label for="pdetails" class="form-label">Product Details</label>
          <textarea
            class="form-control"
            name="pdetails"
            id="pdetails"
            rows="3"
            required
          ><%- data.pdetails %></textarea>
        </div>

        <div class="text-center">
          <button
            type="submit"
            class="btn btn-primary"
            onclick="alert('Product updated successfully...!')"
          >
            Save Product
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
